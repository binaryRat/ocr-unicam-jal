run main.py <input_dir> <output_dir> <options>

options:
 -d --denoising : applies the adaptive threshold on the input images and save them in the output dir
 -o --ocr : applies the ocr algorithm on the input images, if the -d option is present, the script will work on the denoised images

